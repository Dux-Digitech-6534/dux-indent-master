{
  "name": "dux-indent-portal-shell",
  "version": "1.0.0",
  "private": true,
  "description": "Thin Capacitor Android shell that loads the live Dux Procurement Portal desk page (OTA — all UI lives on the Frappe server).",
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0",
    "sharp": "^0.33.5"
  }
}
